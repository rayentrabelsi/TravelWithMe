<html>
<head>


</head>
<body>
<table border="1">
     <tr>
         <td>ID</td>
         <td>Username</td>
         <td>Email</td>
         <td>Actions</td>
     </tr>
      {% for Author in Authors %}
        <tr>
         <td> {{Author.ID}} </td>
         <td> {{Author.Username}} </td>
         <td> {{Author.Email}} </td>

         <td>
             <a href="{{ path('showA', {'id': Author.id}) }}"> Show </a>
           <a href="{{ path('deleteAuthor', {'id': Author.id}) }}"> Delete </a>
           <a href="{{ path('updateAuth', {'id': Author.id}) }}"> Update </a>
         </td>
        </tr>
      {% endfor %}
</table>      