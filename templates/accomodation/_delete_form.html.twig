<form method="post" action="{{ path('app_accomodation_delete', {'id': accomodation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accomodation.id) }}">
    <button class="btn">Delete</button>
</form>
