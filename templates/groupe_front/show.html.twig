{% extends 'base.html.twig' %}

{% block title %}Groupe{% endblock %}

{% block body %}
    <h1>Groupe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe.id }}</td>
            </tr>
            <tr>
                <th>UtilisateurId</th>
                <td>{{ groupe.utilisateurId }}</td>
            </tr>
            <tr>
                <th>NumberMembre</th>
                <td>{{ groupe.numberMembre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_groupe_front_index') }}">back to list</a>

    <a href="{{ path('app_groupe_front_edit', {'id': groupe.id}) }}">edit</a>

    {{ include('groupe_front/_delete_form.html.twig') }}
{% endblock %}
