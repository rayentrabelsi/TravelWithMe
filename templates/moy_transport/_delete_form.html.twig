<form method="post" action="{{ path('app_moy_transport_delete', {'id_transport': moy_transport.idtransport}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moy_transport.idtransport) }}">
    <button class="btn">Delete</button>
</form>
